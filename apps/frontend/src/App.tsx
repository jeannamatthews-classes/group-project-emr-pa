import Login from './pages/Login';

/**
 * Root App component.
 * Renders the Login page at "/" - ready for router expansion when more pages are added.
 */
function App() {
  return <Login />;
}

export default App;
